services:
  - type: web
    name: telegram-bot
    runtime: node
    buildCommand: bash build.sh
    startCommand: npm start
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_USERNAME
        sync: false
